export const metadata = {
  title: 'GLBC',
  description: 'Created by reeeeemo',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
